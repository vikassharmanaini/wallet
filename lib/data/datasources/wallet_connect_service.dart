/// WalletConnect v2 session coordinator (pairing + requests).
///
/// Concrete implementation will be added when `walletconnect_flutter_v2` is
/// integrated without conflicting transitive dependencies.
class WalletConnectService {
  /// Creates a placeholder service.
  const WalletConnectService();

  /// Initializes the WalletConnect client (not yet implemented).
  Future<void> init() async {}

  /// Disconnects all sessions (not yet implemented).
  Future<void> disconnectAll() async {}
}
